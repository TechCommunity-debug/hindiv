{
  "name": "hindivyakaran",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/generate-pdfs.mjs",
    "build:no-pdf": "SKIP_PDF=1 npm run build",
    "preview": "astro preview",
    "og": "node scripts/generate-og.mjs",
    "check": "astro check",
    "astro": "astro",
    "deploy": "npm run build && npx wrangler pages deploy ./dist --project-name=hindi-vyakaran"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.3.1",
    "@astrojs/mdx": "^8.0.1",
    "@astrojs/sitemap": "^3.7.4",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.3.2",
    "tailwindcss": "^4.3.3"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "playwright-core": "^1.63.0",
    "typescript": "^6.0.3"
  }
}
